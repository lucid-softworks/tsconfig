# Contributing

Install the pinned pnpm version and run the complete local check before opening
a pull request:

```sh
corepack enable
pnpm install
pnpm check
```

Keep changes focused on the shared TypeScript configurations and document any
new or changed compiler policy in the README.
